{
  "name": "mcsquishy-blob-on-the-run",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "node --import tsx --test src/game/camera.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vite": "^7.1.5"
  }
}
